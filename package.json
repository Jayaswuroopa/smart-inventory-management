{
  "name": "inventory-manager-root",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd .. && rm -rf public && cp -r frontend/build public",
    "install": "cd frontend && npm install"
  }
}