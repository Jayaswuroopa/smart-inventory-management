{
  "name": "inventory-manager-root",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend",
    "start": "npm start --workspace=frontend",
    "backend": "npm run dev --workspace=backend"
  }
}